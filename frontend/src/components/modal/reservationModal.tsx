import { UserReservation } from "../links/userReservation";
import Modal from "./modal";

export const ReservationModal = () => {
  return (
    <Modal setOpenModal={() => {}} title={"Rezervacija"}>
      {/* <UserReservation role={""} /> */}
    </Modal>
  );
};
